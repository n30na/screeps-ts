/**
 * Created by neona on 7/21/2017.
 */

Room.prototype.canBuild = function(): boolean {

};

Room.prototype.needsToRepair = function(minHealthPercent: number): boolean {
  if(minHealthPercent == null) let minHealthPercent = 0.7;

};
